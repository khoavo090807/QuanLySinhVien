@model IEnumerable<QuanLySinhVien.Models.GiangVien>

@{
    ViewBag.Title = "Danh sách giáo viên";
}

<div class="row mb-4">
    <div class="col-12">
        <h2 class="page-title">
            <i class="fas fa-chalkboard-user"></i> Quản lý Giáo viên
        </h2>
    </div>
</div>

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <span><i class="fas fa-list"></i> Danh sách giáo viên</span>
        <a href="@Url.Action("Create")" class="btn btn-light btn-sm">
            <i class="fas fa-plus"></i> Thêm mới
        </a>
    </div>
    <div class="card-body">
        <!-- Form tìm kiếm -->
        <div class="row mb-3">
            <div class="col-md-6">
                @using (Html.BeginForm("Index", "GiangVien", FormMethod.Get, new { @class = "d-flex" }))
                {
                    <input type="text" name="searchString" class="form-control me-2"
                           placeholder="Tìm kiếm theo mã GV, tên, email..."
                           value="@ViewBag.SearchString" />
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-search"></i> Tìm
                    </button>
                    <a href="@Url.Action("Index")" class="btn btn-secondary ms-2">
                        <i class="fas fa-redo"></i>
                    </a>
                }
            </div>
            <div class="col-md-6 text-end">
                <span class="badge bg-info text-dark fs-6">
                    Tổng số: @Model.Count() giáo viên
                </span>
            </div>
        </div>

        <!-- Bảng danh sách -->
        <div class="table-responsive">
            <table class="table table-hover table-striped">
                <thead>
                    <tr>
                        <th>Mã GV</th>
                        <th>Họ tên</th>
                        <th>Ngày sinh</th>
                        <th>Giới tính</th>
                        <th>Khoa</th>
                        <th>Chức vụ</th>
                        <th>Email</th>
                        <th>Số ĐT</th>
                        <th class="text-center" width="150">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td><strong class="text-primary">@item.MaGV</strong></td>
                                <td>@item.HoTenGV</td>
                                <td>
                                    @if (item.NgaySinh.HasValue)
                                    {
                                        @item.NgaySinh.Value.ToString("dd/MM/yyyy")
                                    }
                                </td>
                                <td>
                                    @if (item.GioiTinh == "Nam")
                                    {
                                        <span class="badge bg-primary">
                                            <i class="fas fa-mars"></i> Nam
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">
                                            <i class="fas fa-venus"></i> Nữ
                                        </span>
                                    }
                                </td>
                                <td>
                                    <span class="badge bg-success">@item.TenKhoa</span>
                                </td>
                                <td>
                                    <span class="badge bg-info">@item.TenChucVu</span>
                                </td>
                                <td>
                                    <small><i class="fas fa-envelope"></i> @item.Email</small>
                                </td>
                                <td>
                                    <small><i class="fas fa-phone"></i> @item.SoDT</small>
                                </td>
                                <td class="text-center">
                                    <div class="btn-group btn-group-sm" role="group">
                                        <a href="@Url.Action("Edit", new { id = item.MaGV })"
                                           class="btn btn-warning" title="Sửa">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a href="@Url.Action("Delete", new { id = item.MaGV })"
                                           class="btn btn-danger" title="Xóa">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="9" class="text-center text-muted py-4">
                                <i class="fas fa-inbox fa-3x mb-3"></i>
                                <p>Không có dữ liệu giáo viên</p>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>